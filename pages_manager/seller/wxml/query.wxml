
<view class="padding-lr  margin-top">
    <view class="text-bold">
        <text class="cuIcon-titles text-yellow"></text>
        <text>查询</text>
    </view>
    
    <view class=" grid col-4 ">
        <view class="padding-tb-xs flex justify-center" wx:for="{{queryList}}" wx:key > 
            <view class="cu-avatar round xl bg-red"  catchtap="clickQuery" data-index="{{index}}">
                <text class="avatar-text text-bold">{{item.name}}</text>
            </view>
        </view>
    </view>
</view>